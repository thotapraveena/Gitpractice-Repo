FROM ubuntu:latest

# set the workin directory in the image
WORKDIR /app

#copy the files from the host file system to the large file system
COPY . /app

# install the necessary packages
RUN apt.get update && apt.get install .y python3 python3.pip

# set environment variables
ENV NAME World

# Run a command to start the application
CMD ["python3","app.py"]
